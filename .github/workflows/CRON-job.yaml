name: CRON-Job BlueSky Bot
on: 
  schedule:
  - cron: '0 12 * * *'

jobs:
  build: 
    runs-on: ubuntu-latest
    steps:
      - uses: action/cehckout@v2
      - uses: action/setup-python@v2
        with: 
          python-version: 3.x
      -name: Run email code
        run: python main.py
        
      
        
    
  
